GATEWAY_NAME=module-install-poc
GATEWAY_HTTP_PORT=9088
ADMIN_PASSWORD=password
TZ=America/Los_Angeles
MODL_PATH=/Users/eknorr/IA/code/projects/ignition-stack/scripts/seeding-poc/.modl-cache/MQTT-Engine-signed-5.0.2.modl

# Variations to flip when iterating the experiment:
# (i)  identifier:   com.cirruslink.mqtt.engine.gateway
# (ii) path:         /usr/local/bin/ignition/user-lib/modules/MQTT-Engine-signed-5.0.2.modl
# (iii) filename:    MQTT-Engine-signed-5.0.2.modl
# (iv) unset (the default below leaves it empty -> not passed to the gateway)
GATEWAY_MODULES_ENABLED=
